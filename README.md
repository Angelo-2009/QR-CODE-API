# QR-CODE-API

A modern, extensible QR Code generation API starter. This branch begins implementation with the core encoding step (feature ##1) and a minimal SVG renderer to expose the module matrix for downstream visual customization.

See docs/ and openapi.yaml for the API contract and next steps.
